---
title: "Před stáží"
format: html
date: 2024/04/10
date-format: long
date-modified: last-modified
editor: 
  markdown: 
    wrap: sentence
---

```{r}
#| label: chunk-setup
#| include: false
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

Abychom společný čas věnovaný stáži dokázali využít naplno, je dobré mít některé věci předem připravené. Níže najdeš seznam potřebného softwaru a základních 

## Required Software Checklist ✅

Before attending SSoQE 2025, please ensure you have the following software installed:

- [ ] **R** (verze 4.5+)
- [ ] **RStudio** (aktuální verze)
- [ ] **Rtools** (pro Windows)
- [ ] **Git** (pro řízení verzí)
- [ ] **GitHub account** (zdarma)

## R and RStudio 💻

- [R](https://en.wikipedia.org/wiki/R_(programming_language)) budeš používat na práci s daty a na jejich analýzu
- 📊 [RStudio](https://posit.co/products/open-source/rstudio/) je prostředí, ve kterém budeš s R pracovat

### Nainstaluj/Aktualizuj R

Make sure you have **R version 4.5 or later** installed. You can:

- Download the latest version from [CRAN](https://cran.r-project.org/)
- Check your current version by running `R.version.string` in R Console

```r
R.version.string
# [1] "R version 4.5.1 (2025-06-13 ucrt)"
```

### Install/Update RStudio

Download and install the **latest version** of RStudio from the [RStudio website](https://rstudio.com/products/rstudio/download/).

You can check your RStudio version by running:

```r
rstudioapi::versionInfo()$version
# [1] ‘2025.5.1.513’
```

### Rtools (Windows Users Only) 🪟

If you're using Windows, you'll need **Rtools** for building R packages. Download the version compatible with your R version from the [CRAN Rtools page](https://cran.r-project.org/bin/windows/Rtools/).

For R 4.5+, use Rtools 4.5 etc.

### Additional Resources

There are plenty of additional guides available to help you:

-   [R Basics for Paleoecologists](https://ckiahtipes.github.io/) by C.A. Kiahtipes
-   [Install or Update R tutorial](https://jennhuck.github.io/workshops/install_update_R.html) by Jennifer Huck


## Git and GitHub 🌐

Please follow the tutorial [here](https://ondrejmottl.github.io/VersionControl_gitHell/) to install Git and set up a GitHub account.

Specifically, we will be using Git, GitHub, and RStudio for version control and collaboration during the workshop.
🤝

### Quick setup checklist

1. **Install Git** - Download from [git-scm.com](https://git-scm.com/)
2. **Create GitHub account** - Sign up at [github.com](https://github.com/)
3. **Configure Git** with your name and email:
   ```bash
   git config --global user.name "Your Name"
   git config --global user.email "your.email@example.com"
   ```
4. **Set up authentication** - We'll help you with Personal Access Tokens (PAT) during the workshop

## Balíčky 📦

Packages are like magic toolboxes 🧰 that contain a collection of functions for specific needs.
We want to make sure that everyone has the necessary packages installed for this workshop.

### Důležité balíčky

Let's create a list of the core packages we'll need from CRAN:

```{r}
#| label: chunk-pkg-list
#| output: false
#| warning: false
#| message: false
# Complete list of R packages used across SSoQE lectures
# Generated on September 11, 2025
ssoqe_package_list <-
  c(
    # Project management & Environment
    "here",
    "remotes",
    "renv",
    "usethis",

    # Data manipulation & Analysis
    "janitor",
    "rlang",
    "tidyverse",

    # Visualization & Graphics
    "showtext",
    "sysfonts",

    # File & Data management
    "fs",
    "jsonlite",
    "rjson",

    # Document generation & Reporting
    "knitr",
    "pander",
    "quarto",
    "tinytable",

    # Regression & Statistical Modelling
    "lme4",

    # Spatial & Geographic analyses
    "sf",
    "terra"

  )
```

### Test if all packages are ready ✅

Let's do a quick test to make sure everything is in order.
Running the following code should produce `"Everything is good to go!"` instead of an error message.

```{r}
#| label: chunk-pkg-test
#| eval: false
missing_packages <-
  installed.packages() %>%
  rownames() %>%
  setdiff(x = ssoqe_package_list)

if (
  length(missing_packages) == 0
) {
  cat("Everything is good to go!")
} else {
  paste(
    "The following packages are not installed: \n\n",
    missing_packages |>
      paste(collapse = ", "),
    "\n\nPlease install them before the workshop."
  ) |>
    warning()
}
```

Now, let's install all the missing packages:

```{r}
#| label: chunk-pkg-install
#| eval: false
# Install packages one by one with error handling
for (pkg in missing_packages) {
  if (!require(pkg, character.only = TRUE, quietly = TRUE)) {
    install.packages(pkg)
  }
}
```


## Potřebuješ pomoc? 🆘


